# G 系列学习与实验

这里保存 G0～G4 的定向实验执行器与实际记录；正文仍是示例的单一维护源。

从仓库根目录执行：

```sh
python3 c++/learning/verify_g.py /usr/bin/clang++ /opt/homebrew/opt/llvm/bin/clang++
```

只使用本机已有编译器，不下载依赖。每次运行创建新的系统临时目录，保存提取源码、命令、诊断和 JSON 结果，不写入历史 PDF 或 FM 证据。只运行带 `g-lab` 标记的完整实验；其余代码块可能是接口片段、示意或明确标出的反例，不能当作独立程序。

先在正文预测，再运行，再解释差异。`PASS` 表示该实验的具体判据满足，不表示整篇文章、整套标准库或全部平台已经验证。编译失败反例必须匹配目标诊断；ASan 反例必须检测到指定错误，崩溃或超时不能替代它。工具缺失记为 `SKIP`，不计通过。

执行器会编译并运行正文里的代码，**不是不可信代码沙箱**。仅运行已经检查过的本仓库实验。

- [回到全系列学习导航](../README.md)
- [本批修订与验证记录](revision-notes.md)
- [本批实际执行结果](verification-results.json)

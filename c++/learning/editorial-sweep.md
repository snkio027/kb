# G0～G12 全书一致性修订与冻结候选

日期：2026-09-27。Base：`b4728e36f307eb4e4c639cdd1300ef7b33f8c929`。本轮依据用户对 Cross-series Editorial Sweep 的批准执行，交付新的 Markdown 内容冻结候选，提交推送后等待集中复审；不启动 PDF Pilot。

## 1. 交付与身份

已接受的 Professional Handbook Source Baseline 继续由 Base 及三批接受记录保存。当前编辑修订不自动继承旧字节的 ACCEPTED：G0～G4 从 1.2 调整为 1.2.1，G5～G12 从 1.1 调整为 1.1.1；Editorial Profile v1.0 保持 ACTIVE BASELINE，不改文件。

- [系列入口](../README.md)先服务学习导航，接受历史和当前状态集中在末尾，不再用历史“待审核”行充当本轮状态。
- [全书阅读约定](../handbook-guide.md)提供术语主讲位置、章节交接、证据词汇和引用身份；它是索引，不是 G13 或第二份编辑规范。
- 十三章统一版本/状态/语言范围/阅读约定及前后章导航，保留原有 H1～H3 与旧定位锚点。
- [冻结候选与检查结果](editorial-sweep-results.json)保存新旧摘要、保护范围、实验提取比对及本轮实际检查，不追写任何历史 JSON。

候选状态为 **FROZEN_CANDIDATE_FOR_REVIEW**：内容按清单固定供本次集中审核，不自行宣布新修订已经接受，不创建发布标签，也不冻结第三方网页。清单包含 13 章及 README、阅读约定、Editorial Profile、实验入口共 17 个源文件；检查记录与清单自身不参与循环摘要。

## 2. 全书级处置

| 审计维度 | 实际处理 | 保留边界 |
| --- | --- | --- |
| 术语 | 统一对象、所有权、表示、同步及时间相关用语 | 代码标识符、英文专有名词及 Gate 原题不机械翻译 |
| 重复论述 | G6 分配/构造、G7 数据竞争回顾与前章摘要改为短承接和精确回查 | 不删除反例前提、局部安全说明、Gate 答案与完整代码 |
| 跨章引用 | 将 G10～G12 的主要概念回查细化到 G1～G9 小节，补齐导航与问题索引 | 不重编号已有章节，不删除历史显式锚点 |
| 章节过渡 | 明确语义→测量→同步→接口→构建→综合→物理系统的交接 | 不增加技术主题或新的实验 |
| 证据用语 | 统一结果标签，区分静态、动态与人工推理 | 原始分类名和历史失败/SKIP 保持，未重跑不写成通过 |
| 引用策略 | 固定 Rust 与 Zig 资料线，另记访问身份 | 不把 URL/访问日期当不可变归档，不宣称所有外链均重查 |
| 批次语言 | 清除 G12 尚称 G10～G12 待初审、Sweep 未启动的旧表述 | 历史记录保留原状态，当前修订仍待集中审核 |

重叠主题定向复核涉及对象/存储、所有权/借用、移动/值类别、分配/构造、机器一致性/HB、关闭协议、二进制/构建合同及证据解释。G6/G7/G12 的共享对象例子分别承担成本、同步和证据审查职责，因此保留，不以相似类型名判定冗余。重复文本扫描、标题/引用清单及差异复核辅助本轮编辑；没有声称逐项重新技术验收全部正文。

## 3. 非阻塞提醒的收口

**G10 生命周期前置条件。** 采用原复审给出的文档合同路线：§2.2 明确公开 `submit`、`close()`、`abort()` 须在 `start()` 成功返回后调用。实现没有检查全部非法转换，合同外调用不承诺拒绝或特定结果；内部启动失败回滚和析构清理单独区分。未实现 RuntimeState，未新增非法调用实验，不把前置条件误报为动态保护。

**G12 Rust 资料固定。** G8/G12 的 Reference、Book、Nomicon 链接固定到 Rust 1.85.0 随附资料；2024 edition 与工具链版本分开。本轮实际读取四页并记录最终 URL、HTTP 状态、字节数和摘要，未运行 rustc。对应概念保持原来的有限比较，不由历史 Reference 推导完整 unsafe 模型或当前实现保证。

**G8 既有指针提醒。** 一并处理 native 接受记录已登记的非阻塞事项：G8-B2 的“不重叠”明确为所指对象范围，覆盖 `dec_create(config, out)` 的配置与输出句柄存储，而不只是首地址。仍是调用方前提，不宣称包装层检测任意指针、重叠或失效。

这三处都是现有合同和引用边界的显式化，不修改实现。G10 的内部回滚仍符合原测试路径；G8 的有效消费者仍满足前提。对合同外路径没有新增运行证据。

## 4. 实际检查与证据分层

结构检查返回 0：38 份 Markdown、961 处本地链接、13 章层级检查无错误；原有 16 项长代码源稿风险保持，仅行号随编辑移动。`git diff --check` 通过。阅读约定与本记录另检查表格显示宽度；没有用原始 URL 长度冒充渲染后的单元格宽度。本轮未进行视觉排版或联网全量链接验收。

一次性只读审计逐章比较 Base 与工作区：953 个围栏块（含代码、Shell、文本图与输出）、1714 个显式锚点、全部标题、98 个实验/文件标记、可见代码身份及 26 个 Gate 题目/答案区域保持原样。G0/G1 Gate 后的完成说明和外部参考不是 Gate 答案区域；不把不同比例的编号行当成题目总数。

另调用四个原执行器的 `extract()`，比较新旧提取对象：30 个实验、68 个文件、实验 ID/mode/判据全部相同。只在内存提取，没有调用它们的编译/执行入口。审计自身用四个小对照确认：普通正文修改可接受，代码、Gate、锚点变动分别被拒绝；它们不是 C++ 技术实验。

对 Base 全部 132 个已跟踪文件逐字节核对：15 个范围内 Markdown 改动，其余 117 个不变。Editorial Profile、FM、全部 design、四个执行器及其测试、结构检查器、历史记录/JSON 和 PDF 均在不变集合。另新增阅读约定、本记录和本轮 JSON 三个文件，不添加构建框架。

| 证据类别 | 本轮实际状态 |
| --- | --- |
| 文档结构/链接与保真 | 运行结构检查、字节/标记/Gate 比对和提取比对；不证明所有技术命题 |
| C++ 编译/负例诊断 | **NOT RUN**；旧源码与判据相同不冒充本轮重跑 |
| 性能观察 | **NOT RUN**；不重复 benchmark，不更新旧数值 |
| 并发动态检测 | **NOT RUN**；不重跑 TSan/stress，不声称新的协议证明 |

执行器 Python 单元回归独立运行 54 项，通过；它们检查提取与判据设施，不记作 C++、性能或并发实验。未安装编译器、依赖或插件。

实际命令（仓库根目录）：

```sh
python3 c++/learning/check_docs.py
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s c++/learning -p 'test_*.py'
PYTHONDONTWRITEBYTECODE=1 python3 /tmp/kb-editorial-sweep.4lT5aR/audit.py
git diff --check
```

临时脚本全文保存在 JSON 的 `audit_script` 字段，重新运行需从仓库根目录执行，并保留 Base 对象；它只读取文件、Git 内容及调用原提取函数，不编译、不写 `dist/`。候选源文件的 SHA-256 位于 `audit.source_snapshot.files`，可直接重算比对。原始网页摘要对应本轮读到的响应字节，不包含网页正文归档。

Rust 历史页面在网页检索工具中不可读，随后经官方 URL 的只读 HTTP 请求成功，四页均未跳转到滚动版本；Zig 0.15.2 页也实际读取。其他外链只盘点，不算联网检查通过。结果分别记录，不把访问工具失败写成文档不存在。

## 5. 冻结与停止边界

本轮完成的是全书级编辑一致性和可复核的内容候选，不是全书重新技术认证。更新后的整篇文档摘要自然不同；历史整篇摘要继续指向原提交，本轮只证明代码与判据保持，不修改旧证据来消除差异。

已声明的平台、性能、动态检测、形式化证明、ROS/硬件、Rust/Zig 执行和物理安全限制全部保留。完整程序长度仍由未来出版层处理，不用删代码消除源稿风险。**PDF NOT BUILT / NOT VALIDATED**；本轮不打开构建/渲染/发布入口。

完成检查、提交与推送后停止，等待本批集中复审。通过后再正式确认全系列 Markdown 内容冻结，并另行启动 G6/G7 PDF Pilot；不增加 G13，不自动推进出版。
